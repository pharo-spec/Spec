SmalltalkCISpec {
  #loading : [
    SCIMetacelloLoadSpec {
      #baseline : 'Spec',
      #directory : 'src',
      #onConflict : #useIncoming,
      #onUpgrade : #useIncoming,
      #ignoreImage : true,
      #onWarningLog : false,
      #platforms : [ #pharo ]
    }
  ]
}